# BCQuality index — domain: `style`

> AUTO-GENERATED by `scripts/build-bcquality-index.ps1`. Do not edit by hand.
> Read this index FIRST, pick the 2-6 relevant slugs by Trigger/Summary,
> then `Read` only those `.md` files. Cite as `[BCQuality: <path>]`.
> Layer precedence: custom > community > microsoft.

| slug | layer | bc-version | triggers (keywords) | summary | samples |
|------|-------|-----------|---------------------|---------|---------|
| affix-as-prefix-on-custom-identifiers | custom | all | affix, prefix, naming, object-name, field-name, table-extension, collision, dyninter | This is the Dynamics International house naming rule. | good/bad |
| abouttitle-abouttext-teaching-tips | microsoft | all | abouttitle, abouttext, teaching-tip, onboarding, page | The `AboutTitle` and `AboutText` properties on a page render a teaching tip — an onboarding callout that appears the first time a user opens the page. | good/bad |
| api-page-camelcase-properties | microsoft | all | api-page, camelcase, apipublisher, apigroup, entityname, entitysetname | API pages — pages declared with `PageType = API` — surface as OData/JSON endpoints. | good/bad |
| api-page-delayedinsert-true | microsoft | all | api-page, delayedinsert, insert-trigger, validation | On a normal page, `DelayedInsert = false` is the default: the record is inserted into the table as soon as the user enters the first field, and subsequent fi... | good/bad |
| api-page-entity-naming-singular-plural | microsoft | all | api-page, entityname, entitysetname, singular, plural | `EntityName` and `EntitySetName` on an API page are the two halves of the OData naming contract. | good/bad |
| api-page-version-format | microsoft | all | api-page, apiversion, version, format, beta | The `APIVersion` property on an API page is part of the public URL path: `/api/<publisher>/<group>/<version>/<entitySetName>`. | good/bad |
| begin-on-same-line-as-then-else-do | microsoft | all | begin, end, compound-statement, aa0005, codecop, formatting | When a compound block follows `then`, `else`, or `do`, the `begin` keyword must sit on the same line as the preceding keyword, separated by exactly one space. | good/bad |
| block-keywords-start-new-line | microsoft | all | block-keyword, end, if, repeat, until, for, while, case, aa0018 | CodeCop AA0018 requires that the block-introducing keywords `if`, `repeat`, `until`, `for`, `while`, `case`, and the block-terminating keyword `end` always s... | good/bad |
| caption-required-on-page-fields | microsoft | all | caption, page-field, aa0225, aa0226, codecop, captionclass | CodeCop AA0225 and AA0226 require every field control to expose a `Caption` property, separately from the field's source name. | good/bad |
| case-action-on-line-after-possibility | microsoft | all | case, statement, formatting, possibility, action, line-break | In an AL `case` statement, the action for each label is written on the line that follows the label, not on the same line. | good/bad |
| error-passes-parameters-directly-not-strsubstno | microsoft | all | error, strsubstno, label, parameters, concatenation, aa0231 | `Error()` accepts a format string and a variable number of arguments — `Error(SomeLabelErr, Arg1, Arg2)`. | good/bad |
| event-subscriber-param-names-match-publisher | microsoft | all | event-subscriber, parameter-name, publisher, signature, eventsubscriber | In AL, an `[EventSubscriber]` procedure is bound to its publisher by event name and parameter list. |  |
| fieldcaption-not-fieldname-in-user-messages | microsoft | all | fieldcaption, fieldname, tablecaption, tablename, translation, message, error | `FieldName` and `TableName` return the developer-facing identifier of a field or table — a fixed English string used in metadata and in code. | good/bad |
| file-name-object-type-pattern | microsoft | all | file-name, object-type, suffix, naming-convention | Each AL source file holds a single object, and the file name is expected to be of the form `<ObjectName>.<ObjectType>.al` — `CustomerCard.Page.al`, `PostSale... |  |
| function-call-parentheses-required | microsoft | all | parentheses, function-call, method-call, aa0008, codecop | AL allows a parameterless procedure to be called without parentheses — `Customer.Init` instead of `Customer.Init()` — and the result is syntactically identic... | good/bad |
| label-comment-explains-placeholders | microsoft | all | label, comment, placeholder, strsubstno, translation, aa0470 | `Label` and `TextConst` strings that contain placeholders (`%1`, `%2`, …) need a `Comment` parameter that names what each placeholder is. | good/bad |
| label-locked-for-non-translatable | microsoft | all | label, locked, translation, token, url, json, xml | A `Label` is by default surfaced to translators and rewritten per locale. | good/bad |
| label-suffix-approved-list | microsoft | all | label, textconst, suffix, aa0074, codecop, msg, err, qst, lbl, tok | CodeCop AA0074 flags `Label` and `TextConst` identifiers that do not end with an approved usage suffix. | good/bad |
| labels-declared-at-object-scope | microsoft | all | label, scope, procedure, translation, localization, xliff | `Label` is the AL declaration that participates in the translation pipeline: the build extracts every Label declared in an object into the `.xlf` file shippe... | good/bad |
| lowercase-reserved-keywords | microsoft | all | reserved-keyword, lowercase, aa0241, codecop, if, then, begin | CodeCop AA0241 requires reserved AL keywords — `if`, `then`, `else`, `begin`, `end`, `var`, `procedure`, `local`, `internal`, `for`, `while`, `repeat`, `unti... | good/bad |
| named-invocations-not-object-ids | microsoft | all | page, report, codeunit, runmodal, run, object-id, named-invocation | `Page.RunModal`, `Report.Run`, `Codeunit.Run`, and the `Page::`, `Report::`, `Codeunit::`, `Table::`, `XmlPort::` selectors accept either a numeric ID or a n... | good/bad |
| no-begin-end-around-single-statement | microsoft | all | begin, end, single-statement, aa0013, codecop, compound | CodeCop AA0013 flags `begin … end` blocks that contain exactly one statement. | good/bad |
| no-else-after-terminating-statement | microsoft | all | else, exit, break, skip, quit, error, terminating, control-flow | When the `then` branch of an `if` ends in a terminating statement — `exit`, `break`, `skip`, `quit`, or `error` — the `else` branch becomes the natural fall-... | good/bad |
| no-space-before-method-parenthesis | microsoft | all | spacing, parenthesis, method-call, aa0002, codecop | CodeCop AA0002 forbids whitespace between a procedure/method name and its `(`. | good/bad |
| object-name-30-char-limit | microsoft | all | object-name, length, prefix, affix, 30-characters, appsource | Business Central object names — for tables, pages, codeunits, reports, queries, XML ports, enums, and permission sets — are limited to 30 characters in total. |  |
| optioncaption-required-and-matches-membercount | microsoft | all | optioncaption, option, member-count, aa0221, aa0223, aa0224 | CodeCop AA0221 requires an `OptionCaption` on every option-type field that is not sourced from a table column (table-sourced option fields inherit the captio... | good/bad |
| page-name-must-match-source-table | microsoft | all | page-name, source-table, misleading, naming | A page (or filtered page View) whose name references one entity but whose `SourceTable` is a different entity misleads every consumer of the object's metadata. |  |
| single-space-after-not-operator | microsoft | all | spacing, not, operator, aa0003, codecop | CodeCop AA0003 requires exactly one space between the `not` operator and the expression it negates. | good/bad |
| single-space-around-binary-operators | microsoft | all | spacing, binary-operator, aa0001, codecop, formatting | CodeCop AA0001 requires exactly one space on each side of every binary operator: assignment (`:=`), arithmetic (`+`, `-`, `*`, `/`, `mod`, `div`), comparison... | good/bad |
| telemetry-event-id-stable-unique | microsoft | all | telemetry, logmessage, event-id, sessionlogmessage, observability | The first parameter of `Session.LogMessage` is the **event ID**. | good/bad |
| temporary-variable-temp-prefix | microsoft | all | temporary, temp, prefix, record-variable, naming | A `Record` variable declared with the `temporary` modifier behaves nothing like a normal record variable: it never touches the database, holds rows only for ... | good/bad |
| this-keyword-in-codeunits | microsoft | all | this, codeunit, self-reference, aa0248, scope | CodeCop AA0248 recommends prefixing self-references inside a codeunit with `this`. | good/bad |
| tooltip-required-on-page-fields | microsoft | all | tooltip, page-field, aa0218, codecop, accessibility, specifies | CodeCop AA0218 requires a non-empty `ToolTip` property on every field control on a page. | good/bad |
| variable-declaration-order-by-type | microsoft | all | variable-declaration, order, var, complex-types, aa0021 | CodeCop AA0021 requires that variable declarations inside a `var` block follow a fixed ordering by type, with complex (composite) types appearing before prim... | good/bad |
| variable-name-must-not-shadow | microsoft | all | variable-name, shadow, conflict, aa0198, aa0202, aa0204, codecop | Three CodeCop rules — AA0198, AA0202, AA0204 — together forbid a local variable from sharing a name with a global variable on the same object, with a field o... | good/bad |
| xmldoc-for-public-library-procedures | microsoft | all | xmldoc, summary, param, returns, public-procedure, documentation | XML documentation comments (`/// <summary>…</summary>`, `/// <param>…</param>`, `/// <returns>…</returns>`) are expected on procedures that form the public s... |  |

_Path pattern: `bcquality/<layer>/knowledge/style/<slug>.md`  -  36 rules._
