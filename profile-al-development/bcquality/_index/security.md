# BCQuality index — domain: `security`

> AUTO-GENERATED by `scripts/build-bcquality-index.ps1`. Do not edit by hand.
> Read this index FIRST, pick the 2-6 relevant slugs by Trigger/Summary,
> then `Read` only those `.md` files. Cite as `[BCQuality: <path>]`.
> Layer precedence: custom > community > microsoft.

| slug | layer | bc-version | triggers (keywords) | summary | samples |
|------|-------|-----------|---------------------|---------|---------|
| classify-every-field-with-dataclassification | community | all | dataclassification, gdpr, privacy, euii, compliance | Every field on every AL table and table extension must have a resolved `DataClassification` value, either declared directly on the field or inherited from a ... | good/bad |
| compose-permission-sets-with-included-sets | community | all | permissionset, includedpermissionsets, assignable, composition, role | The `IncludedPermissionSets` property lets one AL permission set reference another, composing rights out of smaller building blocks. | good/bad |
| do-not-grant-rights-beyond-a-users-entitlement | community | all | entitlement, permissionset, license, clipping, sandbox-drift | Entitlements are license-level caps on what a user can access, derived automatically from the BC license tier. |  |
| guard-bulk-operations-with-istemporary | community | all | istemporary, deleteall, modifyall, safeguard, precondition | An AL helper that accepts a `var Rec: Record X` parameter and performs a bulk operation (`DeleteAll`, `ModifyAll`, or an unfiltered loop that mutates every r... | good/bad |
| prefer-oauth2-over-api-keys-for-external-http-calls | community | all | oauth2, api-key, authentication, httpclient, token-refresh | External HTTP integrations from AL can authenticate using OAuth 2.0 (client-credentials for service-to-service, authorization-code for user-delegated), API k... | good/bad |
| protect-sensitive-data-in-temporary-tables | community | all | temporary-table, data-protection, permission, cleanup | A temporary record copies data out of the source table into session memory. | good/bad |
| al-has-no-built-in-htmlencode | microsoft | all | html, xss, encoding, htmlencode, injection, email | AL does not ship a built-in `HtmlEncode` (or equivalent) function. | good/bad |
| commitbehavior-attribute-scopes-explicit-commits | microsoft | all | commit-behavior, attribute, integration-event, subscriber, commit, atomic | `[CommitBehavior(CommitBehavior::Ignore)]` and `[CommitBehavior(CommitBehavior::Error)]` are method-level attributes that restrict what an explicit `Commit()... | good/bad |
| getlasterrortext-storage-is-privacy-not-security | microsoft | all | getlasterrortext, error-text, classification, privacy, review-scope | It is tempting to flag any code that calls `GetLastErrorText()` and writes the result into a table field (or displays it to end users) as a security issue, o... | bad |
| indirect-permissions-for-elevated-access | microsoft | all | permissionset, indirect-permissions, ri, ii, mi, di, code-mediated | In a `permissionset`, uppercase letters (`R`, `I`, `M`, `D`) grant **direct** permissions: the assignee can read, insert, modify, or delete the table data th... | good/bad |
| inherent-permissions-minimal-grant | microsoft | all | inherentpermissions, inherententitlements, attribute, least-privilege, procedure | `[InherentPermissions(PermissionObjectType::..., ...)]` and `[InherentEntitlements(Entitlement::...)]` are method-level attributes that let a procedure perfo... | good/bad |
| integrationevent-must-not-expose-secrets | microsoft | all | integrationevent, eventsubscriber, secrets, credentials, publisher | `[IntegrationEvent]` publishes a hook that any extension can subscribe to. | good/bad |
| integrationevent-var-parameter-bypasses-security-guards | microsoft | all | integrationevent, var, guard, ishandled, bypass, security-check | A `var` parameter on an `[IntegrationEvent]` is a mutable hook: any subscriber can overwrite the value and the publisher will see the new value when control ... | good/bad |
| isolatedstorage-access-must-be-local-or-internal | microsoft | all | isolatedstorage, local, internal, public, getter, setter, encapsulation | `IsolatedStorage` partitions its data by extension: values written by one extension are unreadable to another. | good/bad |
| isolatedstorage-datascope-module-vs-company | microsoft | all | isolatedstorage, datascope, module, company, user, scope | `IsolatedStorage` read and write methods take a `DataScope` parameter that decides which slice of storage the value belongs to. | good/bad |
| isolatedstorage-setencrypted-for-sensitive-values | microsoft | all | isolatedstorage, setencrypted, encryption, secret, storage | `IsolatedStorage` exposes two write entry points: `Set` stores the value as-is, and `SetEncrypted` stores it encrypted at rest. | good/bad |
| nondebuggable-required-when-unwrapping-secrettext | microsoft | all | nondebuggable, attribute, secrettext, unwrap, debugger | `SecretText` transit — assignment, parameter passing, and return values — is auto-protected: the debugger sees a redacted placeholder, not the value. | good/bad |
| permission-set-avoid-wildcard-grants | microsoft | all | permissionset, wildcard, rimd, tabledata, least-privilege | A `permissionset` object can grant access object-by-object or with the `*` wildcard. | good/bad |
| recordref-open-with-caller-table-must-not-be-public | microsoft | all | recordref, open, public, system-table, scope-onprem, confused-deputy, saas | When a codeunit holds permission to system tables — directly, via a permission set granted at install, or via `[InherentPermissions]` — and exposes a public ... | good/bad |
| secretstrsubstno-for-composing-secrets | microsoft | all | secretstrsubstno, secrettext, strsubstno, format, compose | `SecretStrSubstNo` is the secret-preserving counterpart of `StrSubstNo`. | good/bad |
| secrettext-for-credentials | microsoft | all | secrettext, credentials, api-key, token, debugger, unwrap | `SecretText` is the AL data type for values that should never appear in a debugger session, in a log, or in a variable watch. | good/bad |
| secrettext-with-httpclient | microsoft | all | secrettext, httpclient, setsecretrequesturi, containssecret, headers, http | `HttpClient` and its companion types expose a parallel surface that accepts `SecretText` instead of `Text`, so that secret URIs, secret headers, and secret r... | good/bad |
| validate-user-configurable-urls | microsoft | all | ssrf, uri, url-validation, areurishavesamehost, isvaliduripattern, httpclient | A URL stored in a table field is user-configurable: anyone with write access to the row can change it. | good/bad |
| validatetablerelation-false-on-user-input | microsoft | all | validatetablerelation, tablerelation, field, validation, input | `TableRelation` on a field declares that the field's value must exist in another table; the platform validates the value on entry and on `Validate`. | good/bad |

_Path pattern: `bcquality/<layer>/knowledge/security/<slug>.md`  -  24 rules._
