# BCQuality index — domain: `upgrade`

> AUTO-GENERATED by `scripts/build-bcquality-index.ps1`. Do not edit by hand.
> Read this index FIRST, pick the 2-6 relevant slugs by Trigger/Summary,
> then `Read` only those `.md` files. Cite as `[BCQuality: <path>]`.
> Layer precedence: custom > community > microsoft.

| slug | layer | bc-version | triggers (keywords) | summary | samples |
|------|-------|-----------|---------------------|---------|---------|
| breaking-changes-only-on-tables-without-data | microsoft | all | primary-key, field-type, breaking-change, integer-to-biginteger, existing-data | Primary-key changes and field-type changes (for example widening `Integer` to `BigInteger`) rewrite the on-disk layout of every row in the table. | good/bad |
| datatransfer-for-bulk-init | microsoft | all | datatransfer, large-dataset, bulk-update, modifyall, copyfields, new-field | Tables that can contain more than 300,000 records, and any newly added field on an existing table, should be initialized with `DataTransfer` rather than a `r... | good/bad |
| datatransfer-skips-triggers-and-subscribers | microsoft | all | datatransfer, validate-trigger, event-subscriber, side-effects, business-logic | `DataTransfer` writes directly at the database layer. | good/bad |
| do-not-block-upgrade-on-data-errors | microsoft | all | error-handling, telemetry, session-logmessage, blocking, graceful | When upgrade code encounters unexpected data — a record it expected to find, a relationship it assumed to be intact — the response is to log telemetry and co... | good/bad |
| enum-values-additive-at-end | microsoft | all | enum, ordinal, additive, append, backward-compatible, breaking-change | An AL `enum` is a fixed list of ordinal-named values. | good/bad |
| first-install-dataversion-zero-check | microsoft | all | dataversion, first-install, on-install-app-per-company, moduleinfo, zero-version | On the first install of an extension on a tenant the platform records a zero data version: `AppInfo.DataVersion()` returns `Version.Create('0.0.0.0')`. | good/bad |
| guard-database-reads | microsoft | all | get, findset, findlast, guard, if-then, runtime-error | Inside an upgrade codeunit (or any procedure transitively invoked from `OnUpgradePerCompany` / `OnUpgradePerDatabase`), an unguarded `Record.Get`, `Record.Fi... | good/bad |
| hybrid-migration-codeunits-not-standard-upgrade | microsoft | all | hybrid-migration, hybrid-bc14, hybrid-sl, hybrid-gp, hybrid-base-deployment, one-time-migration | Codeunits like `HybridBC14`, `HybridSL`, `HybridGP`, and `HybridBaseDeployment` implement one-time migration paths from a specific source system into Busines... |  |
| initvalue-does-not-update-existing-rows | microsoft | all | initvalue, new-field, existing-rows, default-value, table-extension | `InitValue` on a field defines the value the platform assigns when a *new* record is inserted. | good/bad |
| minimize-onvalidate-upgrade-triggers | microsoft | all | on-validate-upgrade-per-company, performance-impact, skip-logic, justification, upgrade-tag | Triggers such as `OnValidateUpgradePerCompany` run on every upgrade pass. | good/bad |
| no-external-calls-in-upgrade | microsoft | all | httpclient, dotnet, external-service, network-call, blocking, upgrade-rollback | Upgrade code runs in a constrained execution window: the tenant is mid-upgrade, no users are signed in, and a failure aborts the entire transaction. | good/bad |
| obsolete-pending-to-removed-staging | microsoft | all | obsolete-state, pending, removed, lifecycle, clean-flag, upgrade-code-timing | `ObsoleteState` has a deliberate two-step lifecycle. | good/bad |
| obsoletion-requires-reason-and-tag | microsoft | all | obsolete-state, obsolete-reason, obsolete-tag, deprecation, metadata | When a procedure, field, table, page, or enum value is being retired, AL requires three pieces of metadata to declare the deprecation: | good/bad |
| register-upgrade-tags-with-subscribers | microsoft | all | upgrade-tag, event-subscriber, on-get-per-company-upgrade-tags, on-get-per-database-upgrade-tags, registration | The `Upgrade Tag` codeunit only recognizes a tag if the tag was published to the platform through one of two events on that codeunit: `OnGetPerCompanyUpgrade... | good/bad |
| skip-nonessential-work-via-execution-context | microsoft | all | get-execution-context, execution-context-upgrade, skip, report-selection, runtime-trigger | Runtime procedures (table triggers, install routines, helpers called from many places) sometimes fire during the upgrade window because the upgrade itself to... | good/bad |
| triggers-call-helpers-not-implementations | microsoft | all | on-upgrade-per-company, on-upgrade-per-database, trigger-body, helper-procedure, structure | The `OnUpgradePerCompany` and `OnUpgradePerDatabase` triggers on an upgrade codeunit are dispatch points, not implementation slots. | good/bad |
| upgrade-codeunit-subtype | microsoft | all | upgrade-codeunit, subtype, on-upgrade-per-company, on-upgrade-per-database, trigger | A codeunit only participates in the upgrade pipeline when it sets `Subtype = Upgrade`. | good/bad |
| use-upgrade-tags-not-version-checks | microsoft | all | upgrade-tag, version-check, dataversion, has-upgrade-tag, set-upgrade-tag, control-flow | Each piece of upgrade logic must run exactly once per company (or database) across the lifetime of an extension. | good/bad |

_Path pattern: `bcquality/<layer>/knowledge/upgrade/<slug>.md`  -  18 rules._
