# BCQuality index — domain: `performance`

> AUTO-GENERATED by `scripts/build-bcquality-index.ps1`. Do not edit by hand.
> Read this index FIRST, pick the 2-6 relevant slugs by Trigger/Summary,
> then `Read` only those `.md` files. Cite as `[BCQuality: <path>]`.
> Layer precedence: custom > community > microsoft.

| slug | layer | bc-version | triggers (keywords) | summary | samples |
|------|-------|-----------|---------------------|---------|---------|
| avoid-growing-globals-in-singleinstance-subscribers | community | all | singleinstance, subscriber, event, memory, session | A codeunit with `SingleInstance = true` is allocated once per session and lives until the session ends. | good/bad |
| choose-maintainsiftindex-by-read-write-ratio | community | all | maintainsiftindex, sift, calcsums, flowfield, write-cost | `MaintainSIFTIndex` on a key decides whether the SIFT aggregate structure is updated on every `INSERT`, `MODIFY`, and `DELETE` that touches the key's fields. | good |
| load-common-fields-before-branching-on-case | community | all | setloadfields, case, conditional, branch, field-loading | When record processing branches on state, different branches typically read different fields. | good/bad |
| load-only-primary-key-fields-for-reference-work | community | all | setloadfields, primary-key, reference, existence-check, memory | Work that uses a record only for its identity — passing it to another procedure that will re-fetch what it needs, queueing a key for later processing, runnin... | good/bad |
| omit-filter-only-fields-from-setloadfields | community | all | setloadfields, filter, field-exclusion, index | Fields used only in `SetRange` and `SetFilter` do their work at the database level using indexes; their values never need to be loaded into AL memory for the... | good/bad |
| order-case-branches-by-frequency | community | all | case, branch, frequency, control-flow, hot-path | The AL `case` statement evaluates branches in the order they appear. | good/bad |
| use-deleteall-for-filtered-bulk-deletion | community | all | deleteall, bulk-delete, sql, ondelete, trigger-bypass | `DeleteAll` translates to a single SQL `DELETE` with the record variable's current filters applied as the WHERE clause. | good/bad |
| addloadfields-in-report-onpredataitem | microsoft | all | report, addloadfields, onpredataitem, dataitem, partial-record, layout | Reports iterate dataitems on potentially large source tables and pipe rows into a layout. | good/bad |
| admin-and-migration-pages-tolerate-lower-perf | microsoft | all | admin-page, migration, wizard, hybrid, permissions, lower-severity | Some pages run rarely and against small datasets, and the upstream guidance explicitly calls for treating them as lower severity. |  |
| apply-filters-before-iterating | microsoft | all | setrange, setfilter, filter, loop, early, dataset | A `SetRange` or `SetFilter` placed before `FindSet` narrows the result set at the database. | good/bad |
| apply-guards-before-get | microsoft | all | get, guard, early-exit, conditional, lookup, wasted-query | A `Get` (or any other database call) executed before a guard that may exit the procedure does a round-trip the procedure never uses. | good/bad |
| avoid-commit-inside-loops | microsoft | all | commit, loop, transaction, lock, checkpoint, codeunit-run | Commit ends the current write transaction. | good/bad |
| avoid-get-inside-loop-on-large-table | microsoft | all | n-plus-one, get, findfirst, loop, inner-lookup, large-table | A `Get` or `FindFirst` against a different record inside a loop body produces one database round-trip per iteration — the classic N+1 pattern. | good/bad |
| avoid-recordref-in-hot-loop | microsoft | all | recordref, fieldref, hot-loop, typed-record, metadata | `RecordRef` and `FieldRef` are slower than direct typed record access — the platform resolves the table and field at runtime instead of at compile time. | good/bad |
| avoid-redundant-get-when-record-already-loaded | microsoft | all | get, onaftergetrecord, redundant, page-trigger, rec, already-loaded | A list or card page's `OnAfterGetRecord` trigger fires *because* the platform has already fetched a row into `Rec`. | good/bad |
| avoid-user-prompts-inside-transactions | microsoft | all | confirm, strmenu, dialog, transaction, lock, user-interaction | A `Confirm`, `StrMenu`, modal page, or other user prompt issued from inside a write transaction stalls the transaction — and therefore every lock it holds — ... | good/bad |
| calcsums-instead-of-calcfields-in-loop | microsoft | all | calcfields, calcsums, loop, flowfield, n-plus-one, aggregation | `CalcFields` materializes FlowField values for one record. | good/bad |
| codeunit-run-as-atomic-sub-operation | microsoft | all | codeunit-run, atomic, rollback, transaction, sub-transaction, try-pattern, implicit-commit | `Codeunit.Run(ID)` is the AL-idiomatic way to run a unit of work as an atomic sub-operation with its own transactional boundary. | good/bad |
| codeunit-run-requires-prior-commit-inside-transaction | microsoft | all | codeunit-run, commit, write-transaction, nesting, loop, runtime-error | `Codeunit.Run` cannot nest inside an open write transaction. | good/bad |
| do-not-locktable-in-read-only-procedure | microsoft | all | locktable, read-only, helper, contention, transaction | `LockTable` is a transaction-wide signal: from the call onward, every read against that table in the same transaction acquires `UPDLOCK`. | good/bad |
| do-not-modify-in-onaftergetrecord | microsoft | all | page-trigger, onaftergetrecord, modify, display, scroll, db-write | A list page's `OnAfterGetRecord` fires once per visible row, every time the user scrolls, sorts, or refreshes. | good/bad |
| do-not-remove-sourcetabletemporary-from-api-page | microsoft | all | sourcetabletemporary, api-page, temporary, persistent, in-memory | `SourceTableTemporary = true` on a page makes the page's record buffer in-memory only — reads and writes do not touch SQL. | good/bad |
| findset-true-applies-updlock-on-read | microsoft | all | findset, updlock, readisolation, locking, modify, obsolete-syntax | `FindSet()` and `FindSet(false)` are read-only — no locking. | good/bad |
| flowfield-source-key-needs-sumindexfields | microsoft | all | flowfield, sumindexfields, sift, key, calcformula, aa0232 | A FlowField is computed by SQL on demand. | good/bad |
| guard-event-subscribers-before-db-call | microsoft | all | event-subscriber, guard, db-call, frequently-fired, validate | Event subscribers fire on every event matching their signature — for `OnAfterValidateEvent` on a hot field like `Sales Line.Quantity`, that is every quantity... | good/bad |
| maintainsqlindex-false-breaks-flowfield-sift | microsoft | all | maintainsqlindex, key, sift, flowfield, sum, count, table-scan | `MaintainSQLIndex = false` on a key tells the platform not to materialize that key as a SQL index. | bad |
| pair-findset-with-next-loop | microsoft | all | findset, findfirst, findlast, get, next, repeat-until, aa0181, aa0233 | Two CodeCop rules carve out the loop pattern. | good/bad |
| pass-false-to-insert-when-trigger-not-needed | microsoft | all | insert, modify, delete, trigger, run-trigger, write-parameters | `Insert(true)`, `Modify(true)`, and `Delete(true)` run the table's `OnInsert`/`OnModify`/`OnDelete` trigger; the `(false)` form skips it. | good |
| prefer-dictionary-over-temporary-table-for-lookups | microsoft | all | dictionary, temporary-table, lookup, o-of-1, key-lookup | A temporary table supports a full record API — filters, iteration, multi-field keys — but a pure key→value lookup pays for plumbing it does not use. |  |
| prefer-modifyall-over-per-row-modify | microsoft | all | modifyall, deleteall, bulk, loop, modify, set-based | `ModifyAll` and `DeleteAll` are the bulk APIs. | good/bad |
| prefer-readisolation-over-locktable-for-reads | microsoft | all | readisolation, locktable, updlock, read-only, transaction-scope, isolation-level | `LockTable` and `ReadIsolation` solve different problems with different blast radii. | good/bad |
| production-scale-tables-warrant-extra-analysis | microsoft | all | table-size, hot-table, ledger-entry, item, customer, sales-line, scale | Some Business Central tables routinely reach sizes where access patterns matter much more than they do on a generic table. |  |
| setcurrentkey-aligns-key-with-filters | microsoft | all | setcurrentkey, key, index, filter, sort | The platform chooses a key for each record access. | good |
| singleton-setup-tables-need-no-access-optimization | microsoft | all | singleton, setup-table, sales-receivables-setup, general-ledger-setup, setloadfields, bounded | Business Central setup tables — `Sales & Receivables Setup`, `General Ledger Setup`, `FA Setup`, `Purchases & Payables Setup`, and the broader pattern of any... |  |
| temporary-tables-have-no-database-cost | microsoft | all | temporary-table, in-memory, findset, findfirst, get, no-db-cost | A record declared `Temporary` (or a page with `SourceTableTemporary = true`) lives entirely in memory; reads and writes never reach SQL. |  |
| triggers-and-media-field-regress-modifyall | microsoft | all | modifyall, deleteall, regression, triggers, media, getglobaltabletriggermask, subscriber | `ModifyAll` and `DeleteAll` usually execute as single SQL statements, but the platform falls back to a fetch-then-row-by-row loop under specific conditions. |  |
| understand-implicit-transaction-boundary | microsoft | all | commit, transaction, implicit-commit, write-transaction, runtime, boundary | In AL, write transactions are managed by the runtime, not by the developer. |  |
| use-get-instead-of-findfirst-on-full-primary-key | microsoft | all | get, findfirst, primary-key, setrange, lookup | `Get(...)` is the direct primary-key lookup. | good/bad |
| use-isempty-for-existence-check | microsoft | all | isempty, count, findfirst, existence-check, exists | When the caller only needs to know whether any row matches a filter, `IsEmpty()` is the API designed for the question. | good/bad |
| use-setloadfields-for-partial-records | microsoft | all | setloadfields, partial-record, normal-field, flowfield, get, findset | `SetLoadFields(...)` declares the subset of normal fields the next read should materialize, "reducing data read and transfer thereby improving performance si... | good/bad |
| use-textbuilder-for-string-concatenation-in-loops | microsoft | all | textbuilder, string-concatenation, loop, append, immutable-text | AL `Text` is immutable: each `Result += Piece;` allocates a new buffer and copies the previous content into it. |  |
| use-tryfunction-for-error-catching-not-rollback | microsoft | all | try-function, try-method, error-handling, rollback, atomic, exception, get-last-error, session-buffer | `[TryFunction]` annotates a method so that errors raised inside it can be caught by the caller instead of propagating. | good/bad |

_Path pattern: `bcquality/<layer>/knowledge/performance/<slug>.md`  -  42 rules._
